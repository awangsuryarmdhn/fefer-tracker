{
  "tasks": {
    "start": "deno run -A --env-file=.env main.ts",
    "dev": "deno run -A --env-file=.env --watch main.ts",
    "deploy": "deployctl deploy --project=$DENO_PROJECT --entrypoint=main.ts"
  },
  "compilerOptions": {
    "lib": ["deno.window", "deno.unstable"]
  }
}
