{
  "tasks": {
    "start": "deno run -A main.ts",
    "dev": "deno run -A --watch main.ts",
    "start:env": "deno run -A --env-file=.env main.ts",
    "deploy": "deployctl deploy --entrypoint=main.ts"
  },
  "compilerOptions": {
    "lib": ["deno.window", "deno.unstable"]
  }
}